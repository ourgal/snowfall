local M = {}

-- A whitespace formatter for different query languages
-- Filetypes: { "sql" }
-- Method: formatting
-- Command: sql-formatter
M.formatting = {
  active = true,
  opts = {},
}

return M
