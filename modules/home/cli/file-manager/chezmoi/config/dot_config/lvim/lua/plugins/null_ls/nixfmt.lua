local M = {}

-- nixfmt is a formatter for Nix code, intended to apply a uniform style.
-- Filetypes: {"nix"}
-- Method: formatting
-- Command: nixfmt
M.formatting = {
  active = true,
  opts = {},
}

return M
